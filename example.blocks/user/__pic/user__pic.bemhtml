block user {

    elem pic, content: {
        elem: 'avatar',
        attrs: {
            src : this.ctx.url,
            width : 80,
            height : 80
        }
    }

    elem avatar, tag: 'img'

}